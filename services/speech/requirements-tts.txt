# JMCP TTS sidecar. VoxCPM2 is the default live cockpit voice; Kokoro remains
# the emergency degraded mode. The venv reuses system torch via
# --system-site-packages to avoid duplicate CUDA wheels.
voxcpm>=2.0.0,<3
kokoro==0.9.4
soundfile==0.13.1
